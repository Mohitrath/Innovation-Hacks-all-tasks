'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('demo@flowpilot.dev'),[password,setPassword]=useState('password123'),[error,setError]=useState(''),[busy,setBusy]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await x.json();if(!x.ok)setError(d.error||'Login failed');else r.push('/dashboard');setBusy(false)}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="glass p-8 w-full max-w-md"><div className="text-3xl font-black mb-2">Flow<span style={{color:'var(--accent2)'}}>Pilot</span></div><p className="muted mb-7">AI-powered project & task management.</p><h1 className="text-2xl font-bold mb-5">Welcome back</h1>{error&&<div className="p-3 mb-4 rounded-xl bg-red-950 text-red-200">{error}</div>}<label className="block mb-4"><span className="text-sm muted block mb-2">Email</span><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block mb-6"><span className="text-sm muted block mb-2">Password</span><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn btn-primary w-full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p className="muted text-sm mt-5 text-center">No account? <Link className="text-cyan-300" href="/register">Create one</Link></p><p className="muted text-xs mt-4 text-center">Demo: demo@flowpilot.dev / password123</p></form></main>}
