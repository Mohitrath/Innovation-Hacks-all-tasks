import {NextRequest} from 'next/server';import {z} from 'zod';import bcrypt from 'bcryptjs';import {prisma} from '@/lib/prisma';import {signToken} from '@/lib/auth';import {ok,err} from '@/lib/api';
export async function POST(req:NextRequest){try{const b=z.object({email:z.string().email(),password:z.string()}).parse(await req.json());const u=await prisma.user.findUnique({where:{email:b.email.toLowerCase()}});if(!u||!(await bcrypt.compare(b.password,u.password)))return err('Invalid email or password',401);const res=ok({user:{id:u.id,name:u.name,email:u.email}});res.cookies.set('session',await signToken(u.id),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:604800,path:'/'});return res}catch{return err('Invalid request')}}
