'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function Register(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();const x=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const d=await x.json();if(!x.ok)setError(d.error);else r.push('/dashboard')}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="glass p-8 w-full max-w-md"><h1 className="text-3xl font-black mb-2">Create account</h1><p className="muted mb-7">Start managing projects with FlowPilot.</p>{error&&<div className="p-3 mb-4 rounded-xl bg-red-950 text-red-200">{error}</div>}<label className="block mb-4"><span className="text-sm muted">Name</span><input className="input mt-2" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="block mb-4"><span className="text-sm muted">Email</span><input className="input mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="block mb-6"><span className="text-sm muted">Password</span><input className="input mt-2" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn btn-primary w-full">Create account</button><p className="muted text-sm mt-5 text-center">Already registered? <Link href="/login" className="text-cyan-300">Sign in</Link></p></form></main>}
