import {PrismaClient,Priority,TaskStatus} from '@prisma/client';import bcrypt from 'bcryptjs';const p=new PrismaClient();async function main(){const u=await p.user.upsert({where:{email:'demo@flowpilot.dev'},update:{},create:{name:'Demo User',email:'demo@flowpilot.dev',password:await bcrypt.hash('password123',12)}});const existing=await p.project.findFirst({where:{ownerId:u.id,name:'Launch Website'}});if(!existing){await p.project.create({data:{ownerId:u.id,name:'Launch Website',description:'Ship the new marketing website and analytics.',tasks:{create:[{title:'Finalize landing page',priority:Priority.HIGH,status:TaskStatus.IN_PROGRESS},{title:'Connect analytics',priority:Priority.MEDIUM},{title:'Production QA',priority:Priority.URGENT}]}}})}}main().finally(()=>p.$disconnect());
